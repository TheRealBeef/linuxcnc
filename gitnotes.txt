# This are notes to run some git commands from terminal. 

git init
git config --global init.defaultBranch master
git remote add origin https://github.com/grotius-cnc/linuxcnc.git
git add .
git branch -M master
git push -u origin master

git add .
git commit -m "commit"

git push -f origin master	# this is force, will destroy history.
git push -u origin master    

git pull 					# To update the local clone 
